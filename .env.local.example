# Vercel KV数据库配置示例
# 复制此文件为 .env.local 并填入真实的值

# KV数据库连接信息（从Vercel Dashboard获取）
KV_REST_API_URL=https://your-kv-instance.upstash.io
KV_REST_API_TOKEN=your_secret_token_here

# 可选：只读令牌
KV_REST_API_READ_ONLY_TOKEN=your_readonly_token_here

# 注意：
# 1. .env.local 文件不应该提交到Git仓库
# 2. 在Vercel部署时，需要在Dashboard中设置这些环境变量
# 3. 本地开发时，这些变量会被自动加载